/**
 * minigame/index.js — Public exports for the minigame feature module.
 * Provides the normalized reward model and semantic result keys.
 */
export { normalizeReward, MINIGAME_RESULT } from "./rewardModel.js";
